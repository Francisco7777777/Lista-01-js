let n = parseInt(prompt('Digite um número inteiro positivo n'));

let r = n * (n + 1) / 2;

// O resultado será exibido no console do navegador.
console.log(`A soma dos ${n} primeiros números inteiros positivos é ${r}`);